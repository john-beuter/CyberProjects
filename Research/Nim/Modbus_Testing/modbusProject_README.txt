Nim project designed to work with OpenPLC runtime enable device. Requires Modbus enabled device IP address and coil information.

To run the program, nim must be installed. Please use the following program to compile and run the code:

nim c -r --hints:off .\modbusProject.nim