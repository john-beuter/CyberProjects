package main

import(
	"net"
	"fmt"
	//"strings"
)



//Function should take an IP address as a parameter and enumerate that IP
//Blast it with packets and listen for the connection?
func scanIP(address int){
	//Only listen back on a select IP
	

}





func main(){
	println("test")
}